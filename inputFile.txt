# Input file format for LASDiA software
# The cells starting with the hash tag (#) are commented

# Path sample and bkg file
data_file = './HT2_034T++.chi'
bkg_file = './HT2_036T++.chi'

# Sample composition
xyz_file = './xyzFiles/argon.xyz'         # Path for the XYZ file
# params_file = ''                        # Path molecule parameters file

# Analysis parameters
minQ = 3                                  # Minimum Q value in data interpolation waves
maxQ = 109                                # Maximum Q value in data interpolation waves
QmaxIntegrate = 90                        # Qmax for all integration operations

smooth_factor = 0.25                      # Smoothing factor for cubic spline
damp_factor = 1                           # Exponential damping factor == A*Qmax^2

s = 0.57                                  # Scale factor initial value
rho0 = 26.1                               # Density initial value

# F(r) optimization
# iteration = 2                           # Number of iteration for F(r) optimization
# rmin = 0.22                             # The distance below which no peaks in F(r) may occur

# Chi2 minimization
# s_min =                                 # Scale factor minimum value for the minimization
# s_max =                                 # Scale factor maximum  value for the minimization
# s_step =                                # Scale factor step value for the minimization

# rho0_min =                              # Density minimum value for the minimization
# rho0_max =                              # Density maximum  value for the minimization
# rho0_step =                             # Density step value for the minimization

# Ploting/Writing variables: [Plot, Write]
# plot_raw_data = ['N', 'N']
# plot_S_Q = ['N', 'N']
# plot_S_Qsmoothed_damped = ['N', 'N']
# plot_F_r = ['N', 'N']
# plot_F_r_corr = ['N', 'N']
# plot_S_Q_corr = ['N', 'N']
# plot_s_chi2 = ['N', 'N']
# plot_rho0_chi2 = ['N', 'N']
