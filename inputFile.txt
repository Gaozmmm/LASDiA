# Input file format for LASDiA software
# The lines with the hash tag (#) are comments

# Path sample and bkg file
data_file = "../data/cea_files/Ar/HT2_034T++_rem.chi"
bkg_file = "../data/cea_files/Ar/HT2_036T++_rem.chi"

# Sample composition
xyz_file = "./xyzFiles/argon.xyz"         # Path for the XYZ file
molecule = "Ar"                           # Molecular composition: Ar, CO2, N2
# params_file = " "                         # Path molecule parameters file

# Analysis parameters
minQ = 3                                  # Minimum Q value in data interpolation waves
QmaxIntegrate = 90                        # Qmax for all integration operations
maxQ = 109                                # Maximum Q value in data interpolation waves

smooth_factor = 0.25                      # Smoothing factor for cubic spline
damp_factor = 1                           # Exponential damping factor == A*Qmax^2

# F(r) optimization
iteration = 2                           # Number of iteration for F(r) optimization
rmin = 0.22                             # The distance below which no peaks in F(r) may occur

# Chi2 minimization
s_min = 0.57                              # Scale factor minimum value for the minimization
s_max = 0.57                              # Scale factor maximum  value for the minimization
s_step = 1                                # Scale factor step value for the minimization

rho0_min = 26.1                           # Density minimum value for the minimization
rho0_max = 26.1                           # Density maximum  value for the minimization
rho0_step = 1                             # Density step value for the minimization

# Ploting/Writing variables: [Plot (Y/n), Write (Y/n), Path] 
pw_raw_data = ["y"]
pw_S_Q = ["n", "n", "./test/SQ.txt"]
pw_S_Qsmoothed_damped = ["n", "n", "./test/SQsmooth.txt"]
pw_F_r = ["N", "N", "./test/Fr.txt"]

pw_F_r_iter = ["n"]

pw_F_r_corr = ["N", "N", "./test/Fr_corr.txt"]
pw_S_Q_corr = ["N", "N", "./test/SQcorr.txt"]
pw_s_chi2 = ["N", "N", "./test/sChi2.txt"]
pw_rho0_chi2 = ["N", "N", "./test/rho0Chi2.txt"]
